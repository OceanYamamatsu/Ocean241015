import tkinter as tk
root = tk.Tk()
root.title("title")
root.geometry("240x160")

frm = tk.Frame(root)

lb =  tk.Label(frm,text="こんばんは")
lb.pack(side=tk.LEFT,padx=20)

ent=tk.Entry(frm,width=10)
ent.pack()
frm.pack(fill=tk.X)




root.mainloop()